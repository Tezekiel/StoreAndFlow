<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewmodel"
            type="com.clean.cut.quiztactoe.viewmodel.LocalGameViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.LocalGameActivity"
        android:background="@drawable/background_pacific_dream">

        <TextView
            android:id="@+id/winOrDrawTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text='@{viewmodel.winOrDraw.equals("draw") ? @string/draw : viewmodel.winOrDraw.equals("win") ? @string/winner : ""}'
            android:visibility="@{viewmodel.gameOver ? View.VISIBLE : View.GONE}"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="30dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{viewmodel.gameOver ? View.GONE : View.VISIBLE }"
            android:text="@{@string/now_playing(viewmodel.currentlyPlaying)}"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="30dp"/>

        <TextView
            android:id="@+id/winnerTv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewmodel.winner.name}"
            android:visibility="@{viewmodel.gameOver ? View.VISIBLE : View.GONE}"
            android:textSize="24sp"
            android:layout_marginTop="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/winOrDrawTv" />

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/primaryDarkColor"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <GridView
                android:id="@+id/boardGv"
                android:layout_width="400dp"
                android:layout_height="400dp"
                android:horizontalSpacing="4dp"
                android:verticalSpacing="2dp"
                android:scrollbars="none"
                app:OnItemClickListener="@{(parent, view, position,id)->viewmodel.onItemClickListener(parent, view, position,id)}" />

        </RelativeLayout>

        <com.google.android.material.button.MaterialButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{(view) -> viewmodel.resetGame()}"
            android:text="Play again?"
            android:textSize="16sp"
            app:layout_constraintBottom_toBottomOf="parent"
            android:visibility="@{viewmodel.gameOver ? View.VISIBLE : View.GONE}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>